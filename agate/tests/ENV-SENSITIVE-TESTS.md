# 环境敏感测试集中清单

> TAG0023 RM-AG0044（BDD-10）。登记因外部环境（平台/工具版本）差异而非被测代码缺陷导致
> 结果不稳定的测试用例，供统一追踪与后续根因治理参照。判定标准（P2-design.md §2.3）：
> 测试断言/前置条件依赖"外部环境计算结果的长度/内容会因平台或工具版本产生非代码逻辑
> 相关的差异"（如 git 版本差异导致 short hash 长度不同、临时目录挂载点/basetemp 位置差异），
> 且该差异不代表被测代码本身存在缺陷 → 归类为"环境敏感测试"。

| test_id | 根因分类 | 状态 | 关联 |
|---------|---------|------|------|
| test_bdd_7 | basetemp位置依赖 | 已根治 | RM-AG0041 |
| test_bdd_25 | basetemp位置依赖 | 已根治 | RM-AG0041 |
| test_bdd_14 | git版本差异 | 已根治 | RM-AG0044，TAG0023 |

## 字段说明

- **test_id**：测试函数名（不含文件路径，跨文件重名以首次登记为准）。
- **根因分类**：`basetemp位置依赖` / `git版本差异` / 其他（新增分类需在本节补充说明）。
- **状态**：`已根治`（根因已定位并修复，回归验证通过）/ `观察中`（已知环境敏感但尚未定案根治方案）。
- **关联**：对应的 RM 编号或修复 commit 引用。

## 与既有 P5 回归判定机制的边界

`agate/phase-cards/P5-verification.md` 描述的 P5 机械化回归判定机制
（`pre-task-baseline.md` + `known-failures.md`；`LIMITATIONS.md` 局限 3 亦提及为一条缓解）
解决的是"任务前后两次测试快照
diff，只拦截新增失败"这一不同维度的问题——不区分失败是否环境敏感，只判定是否为
本任务新引入。本清单是正交的分类账本：登记"哪些测试已知对环境敏感、敏感在什么
维度"，两者不合并、不互相替代。
