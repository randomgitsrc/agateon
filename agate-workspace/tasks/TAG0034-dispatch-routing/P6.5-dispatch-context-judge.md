---
phase: P6
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0034
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）

### 目标

P6.5 独立裁判：以 **fresh context** 逐条重验 `P1-requirements.md` §4 的**全部 53 条** `#### BDD-NN`（含 P6 已判 PASS 项，**零挑验**）。只凭标准（P1 BDD 的 Given/When/Then 判据 + `P2-design.md` 验收相关节）+ `P6-evidence/` 下证据文件 + 你自己实跑的命令 + `git log` 判案。产出 `P6.5-judge-verdict.md`。**只审不写**——不改进程 / 不改代码 / 不碰 git 历史。

### 认知约束（四条强制）

1. **逐条重验所有 53 条 BDD**：verdict 正文须给 53 条独立结论行 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: {描述} ({证据路径})`。`criteria_total` 必须 == 53（`grep -c '^#### BDD-' P1-requirements.md`）。挑验 → `check-judge-verdict.py` 机械拦截。
2. **只信证据与 git log**：不读、不引用 `P6-acceptance.md`（verifier 自述，信息隔离）。每条结论依据 = `P6-evidence/` 证据文件内容 + 你实跑命令的输出 + `git log` / `git diff` 可查证记录。
3. **每条结论引用证据路径**：引用须在 `verdict_evidence` 清单内、指向真实存在且非空的文件（`P6-evidence/` 下 或你落盘的实跑日志）。
4. **禁止「看起来没问题」**：证据不足以判定 → 判 `FAIL` 或 `NEEDS-REVISION`，不猜。

### 可用证据 + 你可自行实跑的核验

`P6-evidence/` 现有 4 文件（均非空）：
- `pytest-tag0034.log` —— `pytest agate/tests/ -k tag0034 -q` 全量输出（P6 verifier 跑，末行 `EXIT_CODE`）
- `doc-assertions.log` —— 文档断言类 BDD 的 grep 命中行（design-note / roadmap / dispatch-protocol.md / architect.md / platform-notes.md）
- `regression-zero-change.log` —— `git diff --stat f75e129..HEAD` 对 `phases.yaml` / `check-gate.py` / `check-state-transition.py` / `state-machine.md` 的零改动 + `zero_change` 测试实跑
- `bdd-42-platform-independence.log` —— `check-judge-verdict.py` / `check-p6-provenance.py` 无平台 transcript 路径 + 任务范围零改动

**你应自行实跑复核（不能只信上述日志）**：
- **功能类 BDD（大多数）**：自己跑 `timeout 400 python3 -m pytest agate/tests/ -k tag0034 -q`（预期 90 passed / 0 failed），把输出落 `P6.5-judge-evidence/pytest-rerun.log`（末行 `EXIT_CODE: <n>`）——这是你自己的证据，比 verifier 的日志更可信。逐条 BDD 对应的测试函数名在 `P1-requirements.md` 各 BDD 的 Then 里可推断 / 在 `pytest -k "bdd_NN" -v` 单跑确认。
- **文档断言类 BDD-41/45/46/47/48/49/52/53 + BDD-10 platform-notes 部分**：自己 `grep -nE '<关键串>' <目标文件>` 复核（目标文件见下「输入文件」）——不能只信 `doc-assertions.log`。关键串以 `P1-requirements.md` 对应 BDD 的 Then 子句为准。落 `P6.5-judge-evidence/doc-recheck.log`。
- **回归护栏 BDD-39/40**：自己跑 `git diff --stat f75e129..HEAD -- agate/rules/phases.yaml agate/scripts/check-gate.py agate/scripts/check-state-transition.py agate/state-machine.md`（应无输出）+ `grep -c dispatch_route agate-workspace/tasks/TAG0034-dispatch-routing/gate-events.jsonl`（应 = 0，`gate-events.jsonl` 在白名单）+ `pytest agate/tests/regression/test_tag0034_zero_change.py -q`。**BDD-40 的「全量回归全绿」部分**：自己跑 `timeout 600 python3 -m pytest agate/tests/ -q --tb=no`（预期 1625 passed / 0 failed / 2 skipped），落 `P6.5-judge-evidence/full-suite.log`——**不要引用 `P5-test-results/`**（黑名单）。
- **BDD-42**：自己跑 `grep -nE '\.codex/sessions|\.claude/projects|expanduser|CODEX_HOME' agate/scripts/check-judge-verdict.py agate/scripts/check-p6-provenance.py`（应 0 命中）+ `git log --oneline f75e129..HEAD -- agate/scripts/check-judge-verdict.py agate/scripts/check-p6-provenance.py`（应无 commit）。
- **BDD-24~28（模型购物完整性洞，known_risks 最高危）**：证据要扎实——自己跑 `pytest -k "bdd_24 or bdd_25 or bdd_26 or bdd_27 or bdd_28" -v` + `grep -nE 'GATE_FAIL|gate_fail' agate/scripts/agate_dispatch_route.py agate/scripts/check-events.py`（`agate_dispatch_route.py` 的 `Outcome.kind` / `.reason` 枚举**不含** `gate_fail`；`check-events.py` 第 8 条**拒绝** `gate_fail`——两者语义相反，核对是否与 BDD-27 一致）。

### 三档预算（超限诚实降级）

- 复核轮次 ≤2（账本 `judge_verdict` 事件计数机械兜底）
- token 100k（`.state.yaml` `judge.judge_token_budget` 可覆盖，本任务未设 → 100k）
- 时间 30min wall-clock

预算耗尽 → 立即停、按已验条目落盘、`status: needs-revision` + `partial: true`，结论区说明哪些未验完。**不得** `status: passed` 静默放行。

### verdict 产出格式（`P6.5-judge-verdict.md`）

frontmatter：`status`（passed/rejected/needs-revision）+ `criteria_total: 53` + `criteria_passed: <N>`（`passed` 时须 == 53）+ `verdict_evidence: [...]`（每条结论引用 ⊆ 此清单、且清单每项被引用）+ `partial: false`。
正文 53 行 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: {描述} ({证据路径})`，BDD 编号 1~53 齐全。
用 `agate-md-field-set` 写 frontmatter（`status` / `criteria_total` / `criteria_passed` / `partial`；`verdict_evidence` 是 JSON list，若 `agate-md-field-set` 不支持则手工加为合法 YAML list 后 `check-frontmatter.py` 验证）。

### 约束

- **信息隔离**：本 dispatch-context 只含白名单输入（P1-requirements.md / P2-design.md 验收节 / P6-evidence/ / .state.yaml / gate-events.jsonl + git log 权）。你**不得**主动去读 `P6-acceptance.md` / `P4-implementation*.md` / `P4-review.md` / `P5-test-results/` / 任何 `P*-dispatch-context-*.md`（judge 自己这份除外）。
- **只审不写**：不改代码 / 测试 / 协议文档 / git 历史。落盘只写 `P6.5-judge-verdict.md` + `P6.5-judge-evidence/` + `P6.5-progress.md`。
- **PROD**：dogfooding，"生产" = 主 checkout `/home/kity/oclab/agateon` + `~/.agate`，禁改。仅 worktree 内读 + 跑脚本。`[PROD_NOT_TOUCHED]` / `[PROD_TOUCHED]` 二值声明。
- **命令超时兜底**：所有 bash 前 `timeout <n>s`（全量 pytest 600；tag0034 400；单条 60-120）。挂死 → 停、progress 写一行、返回主 Agent。
- **格式**：verdict 正文的结论行用 `- PASS` / `- FAIL` / `- NEEDS-REVISION` 前缀（这是 judge verdict 的规定格式，`check-judge-verdict.py` 按此正则解析——与 P6-acceptance 的 provenance 预判限制不同，judge verdict 就该用这个前缀）。

> 子派发能力：不启用（judge 类角色不适用子派发）。

### 上游关联

- 任务状态字段：phase=P6 / `judge.enabled: true`（机制强制，P1 created 2026-09-09 ≥ 截止 2026-08-22）/ `p5_pass_commit` 已写入。P6.5 是 P6 的子阶段，phase 值保持 P6。
- 事件账本现约 18 行，哈希链完整；含 P0–P6 的 gate_run / state_transition + 一条 P4→P5 主 Agent 手动推进的 state_transition。无 judge_verdict 事件（本轮是首轮 judge）。
- 标准（P1）：53 条 BDD（编号连续 1~53）。BDD-10 是 `[BASELINE_CHANGE]`（用户 2026-09-09 批准：effort 按 `claude --help` 能力探测分流映射 `--effort`）。
- **不在 P6.5 验收范围**（P1 §9 已登记为 P8 交付项，勿判 FAIL）：局限 2 缓解链补写 + ADR-013 补写 + tmux 观测层目标环境复跑，共三项，均 P8 落地；P1 的 BDD 只到 53，你只逐条验 BDD-1~53。
- git log 可查证（`f75e129..HEAD` 范围）：P1..P6 逐阶段 commit（P1 `f75e129` / P6 HEAD `eb924be`）；P4 内部三批 commit。

### 输入文件（白名单，只传路径）

- `agate-workspace/tasks/TAG0034-dispatch-routing/P1-requirements.md`（**53 条 BDD —— 判案唯一标准**）
- `agate-workspace/tasks/TAG0034-dispatch-routing/P2-design.md`（**仅验收相关节** —— §10「实现完成的标志」13 条 + §3 各节 Then 判据基准 + §8 gate_commands；其余节可略）
- `agate-workspace/tasks/TAG0034-dispatch-routing/P6-evidence/`（证据文件 —— 目录）
- `agate-workspace/tasks/TAG0034-dispatch-routing/.state.yaml`
- `agate-workspace/tasks/TAG0034-dispatch-routing/gate-events.jsonl`
- git log / git diff 查询权（`f75e129..HEAD` 范围）

> 复核每条 BDD 的方式是**你自己实跑 pytest / grep / git diff**（要跑什么命令、grep 什么措辞、对哪个协议文件 —— 逐条列在上面「可用证据 + 你可自行实跑的核验」节）。角色定义与仓库约定见本 prompt 已指定的必读项。

### 产出文件

- `agate-workspace/tasks/TAG0034-dispatch-routing/P6.5-judge-verdict.md`（verdict）
- `agate-workspace/tasks/TAG0034-dispatch-routing/P6.5-judge-evidence/`（你自己实跑的 pytest-rerun.log / doc-recheck.log / full-suite.log / bdd-24-28-recheck.log 等，末行 `EXIT_CODE: <n>`）
- `agate-workspace/tasks/TAG0034-dispatch-routing/P6.5-progress.md`（分阶段落盘）
</dispatch_guide>

<!-- AGATE_CARD_START -->
P6.5 是 P6 的子阶段，无独立阶段卡片。阶段规范见 `agate/phase-cards/P6-acceptance.md`「P6.5 judge 复核（强制，所有任务）」节 + `agate/assets/review-roles/judge.md`（角色定义，本 prompt 已指定为强制遵循）。
<!-- AGATE_CARD_END -->

<objective_info>
- 环境状态：worktree `.worktrees/agate-TAG0034`（分支 `feat/TAG0034-dispatch-routing`）；`.state.yaml` phase=P6 / judge.enabled=true / p5_pass_commit=92edcfc；HEAD = `eb924be`（P6）
- P1 BDD 计数：`grep -c '^#### BDD-' agate-workspace/tasks/TAG0034-dispatch-routing/P1-requirements.md` = 53 —— `criteria_total` 必须 = 53
- 主 Agent 已跑（**你须自行实跑复核**）：`pytest agate/tests/ -k tag0034 -q` = 90 passed / 0 failed；`pytest agate/tests/ -q --tb=no` = 1625 passed / 0 failed / 2 skipped；`git diff f75e129..HEAD -- agate/rules/phases.yaml agate/scripts/check-gate.py agate/scripts/check-state-transition.py agate/state-machine.md` 无输出；`grep -c dispatch_route .../gate-events.jsonl` = 0
- `P6-evidence/`：pytest-tag0034.log（9.4K）/ doc-assertions.log（23.8K）/ regression-zero-change.log / bdd-42-platform-independence.log
- gate-events.jsonl：无 `judge_verdict` 事件（首轮 judge）；`check-events.py` 现 exit 0
- 放行门槛（主 Agent 跑）：`check-gate.py P6.5 agate-workspace/tasks/TAG0034-dispatch-routing` = `check-judge-verdict.py` + `check-events.py` 双 exit 0
- 工具：python 3.12.3 / pytest 9.0.3 —— 全量套件约 170-180s（`timeout 600`），tag0034 约 3-5s
- 任务目录名：`TAG0034-dispatch-routing`（完整目录名）
</objective_info>

> 注：本文件是 judge 的白名单 dispatch-context —— 不含任何黑名单路径（`P6-acceptance.md` / `P4-implementation*.md` / `P4-review.md` / `P5-test-results/` / `P*-dispatch-context-*.md`）。
