---
phase: P6.5
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0033
role: judge
---

<dispatch_guide>
> 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 大于 客观查证信息 大于 阶段卡片。
> 你以 **fresh context** 独立复核 TAG0033 的 P6 验收——不看 verifier 的验收自述，只信**证据文件**与
> **git log**。目的是打破「评审者与作者同信任链」的锚定。

### 目标

对 TAG0033（Codex 命令流适配器）的 **30 条 BDD（BDD-1~30）逐条独立复核**，产出
`agate-workspace/tasks/TAG0033-codex-cmdstream-adapter/P6.5-judge-verdict.md`。含**已 PASS 项也要重验**
（不采信"上一步说 PASS"）。判定依据 = `P6-evidence/` 下的实跑证据日志 + `git log` 查询 + 你自己重跑
（允许重跑 pytest / grep / git diff / check 脚本——这些是可复现的客观命令）。

### 每条 BDD 的复核方法（BDD 文本见 P1-requirements.md §6，逐条 Given/When/Then）

- **BDD-1~17**（probe / list_sessions / read_commands 字段映射 / spawn_agent 子会话 / 检测引擎三态）：
  读 `P6-evidence/bdd-01.log`~`bdd-17.log`（verifier 的 `pytest -v` 实跑输出）；**你自己重跑**对应
  `python3 -m pytest -v agate/tests/unit/test_agate_cmdstream_adapters.py::<test_id>` /
  `test_agate_cmdstream_detect.py::<test_id>`（test id 从证据 log 里的用例名取，或
  `python3 -m pytest --collect-only -q agate/tests/unit/test_agate_cmdstream_adapters.py agate/tests/unit/test_agate_cmdstream_detect.py | grep -iE "codex"`）确认 PASSED。
- **BDD-18**（全量单测全绿）：读 `bdd-18.log`；自己重跑 `timeout 300s python3 -m pytest agate/tests/unit/ -q`
  确认 `passed / 0 failed`（当前基线 1390 passed / 0 failed / 2 skipped）。
- **BDD-19**（`test_bdd_6_detect_consumes_registry_zero_change` 断言改包含式）：读 `bdd-19.log`；自己
  `git diff main...HEAD -- agate/tests/unit/test_agate_cmdstream_adapters.py` 确认 `assert registered == {三键}`
  → `.issubset(registered)`（或 `>=`）+ `assert "codex" in registered` 存在。
- **BDD-20**（consistency 0 ERROR）：读 `bdd-20.log`；自己
  `timeout 120s python3 agate/scripts/check-protocol-consistency.py --strict-errors-only` 确认 exit 0 / 0 ERROR。
- **BDD-21**（检测引擎 / IR / 阈值 / 既有三适配器零改动）：读 `bdd-21.log`；自己
  `git diff main...HEAD -- agate/scripts/agate-cmdstream-detect.py agate/scripts/agate-cmdstream-ir.py`
  确认**为空**；`git diff main...HEAD -- agate/scripts/agate-cmdstream-adapters.py --stat` 确认只有新增
  （insertions，deletions=0 或仅注释/docstring），既有 `ClaudeCodeAdapter`/`OpenCodeAdapter`/`DSHAdapter`
  class 体无删改；`pytest -v ...::test_bdd_21_*` 两守护 PASSED。
- **BDD-22~27**（platform-notes.md Codex 章 / SETUP.md Codex 小节内容锚点）：读 `bdd-22.log`~`bdd-27.log`；
  自己 `pytest -v agate/tests/unit/test_codex_platform_docs.py::<test_id>` + `grep -nE "<锚词>" agate/platform-notes.md`
  （BDD-22~26）/ `agate/SETUP.md`（BDD-27）确认文档实际写了。锚词从 P1 §6.6/6.7 各 BDD 的 Then 取。
- **BDD-28**（SELF-GATE 留痕 + protocol-alignment-review 存在）：读 `bdd-28.log`；自己
  `git log --oneline --grep="self-gate-review\|self-gate-skip"` 确认 SELF-GATE 触发的 commit 有前缀行；
  `ls agate-workspace/reviews/agate-alignment-review-2026-09-09-TAG0033*.md`（3 个）+ `grep -n "reviewer\|agent" <各文件首部>`
  确认 `agent != main`；consistency 0 ERROR。
- **BDD-29**（真机验证清单成形四要素）：读 `bdd-29.log`；自己
  `grep -nE "^\| V[1-8] " agate-workspace/tasks/TAG0033-codex-cmdstream-adapter/P1-requirements.md` 确认 §7 表
  V1-V8 每行含「验证什么 / 怎么验 / 阶段 / 通过判据」+ V8 标「待有该环境时补（非阻塞）」+
  `verification_env_budget` 占位。
- **BDD-30**（spawn_agent schema 穷尽项已登记 + 方法边界注明）：读 `bdd-30.log`；自己
  `grep -nE "穷尽|spawn_agent.*schema|逐字输出|V2" agate-workspace/tasks/TAG0033-codex-cmdstream-adapter/P1-requirements.md`
  确认 §7 含该项 + 注明 P1「令模型逐字输出内部 tool schema」不可用 + P5-P6 换法 + 通过判据。
- **真机三态佐证**（BDD-13~17 相关）：读 `P6-evidence/real-machine-v6-frozen.log` / `-normal.log` /
  `-spin.log` / `real-machine-p6.md`——确认真实 Codex 会话 detect 得 ① FROZEN ② NORMAL ③ SPIN。
  ③ SPIN 是 F1（DEBT0035）修复的关键证据——真机 `status="failed"` 重复失败命令经 `read_commands` 产出
  真实 `exit`/`output_hash` → detect 判 SPIN。你可自己对证据 log 里记的 rollout 路径（若文件还在）重跑
  `python3 agate/scripts/agate-cmdstream-detect.py detect <rollout> --platform codex --now <ts>` 复核。

### 约束

1. **fresh context 独立复核**——不读、不引用 verifier 的验收自述文件（P6 阶段主产出）；不读任何
   `P{4,5,6}-dispatch-context-*.md` / `P4-implementation.md` / `P4-review.md` / `P5-test-results/`
   （黑名单，`check-judge-verdict.py` 会扫本文件「输入文件」「上游关联」两节，含黑名单 → exit 1）。
   证据来源限：`P6-evidence/` + `git log` + 你自己重跑客观命令 + `P1-requirements.md` + `P2-design.md`
   （仅验收相关节：§5 设计点定论 / §6 BDD / §7 真机清单）。
2. **重跑客观命令是允许且鼓励的**——pytest / grep / `git diff` / `git log` / `check-*.py` 都可复现，
   跑它们不算"看作者产出"。
3. **逐条结论**——BDD-1~30 每条给 `- PASS BDD-N: {描述} ({证据文件名})` 或 `- FAIL BDD-N: ...`。
   证据文件名用**半角括号**、括号内**只放** `P6-evidence/` 下的文件名（不带目录前缀，可逗号分隔多个）——
   `check-judge-verdict.py` 只识别半角 `()` 且括号内仅文件名。补充说明写括号外。
4. **frontmatter**：`status`（`passed` / `needs-revision`）、`criteria_total: 30`、`criteria_passed: <int>`、
   `verdict_evidence:`（非空列表，30 个证据文件名，不带 `P6-evidence/` 前缀）、`partial: false`
   （预算未耗尽）、`phase: P6.5`、`task_id: TAG0033`、`created: 2026-09-09`、`agent: judge`。
5. **预算**：复核轮次 ≤2 / token 100k / 时间 30min。耗尽 → `status: needs-revision` + `partial: true`。
6. **本文件「输入文件」「上游关联」两节禁含行首 `- PASS` / `- FAIL`**（触发 dispatch-context 验收结论
   预判扫描 → exit 1）。你在 `P6.5-judge-verdict.md` 正文里当然要写真实的 `- PASS BDD-N:` / `- FAIL BDD-N:`。

### 上游关联

- 本任务是「给 RM-AG0055 命令流机制加 CodexAdapter + 补 Codex 平台文档」，30 条 BDD 分布：BDD-1~12 =
  CodexAdapter 解析单测；BDD-13~17 = 检测引擎对 Codex 会话三态；BDD-18/20 = 回归底线；BDD-19/21 =
  零改动守护；BDD-22~27 = 平台文档内容；BDD-28 = SELF-GATE 留痕；BDD-29/30 = 真机验证清单结构。
- 过程中曾 P5→P4 单步回退修 F1（DEBT0035，`git log --grep=retreat` / `--grep=DEBT0035` 可查）——真机
  Codex `status="failed"` 终态未被原 pending 判据识别，已修（新增 `_codex_is_finished` helper）；BDD-5/6/15
  的 Given 有 `[BASELINE_CHANGE]` 注记（P1 §6，主 Agent 授权）。你复核 BDD-5/6/15 时以 P1 §6 现文本为准。

### 输入文件（按顺序读——仅白名单）

1. `agate-workspace/tasks/TAG0033-codex-cmdstream-adapter/P1-requirements.md`（§6 全 30 BDD 原文 + §7 真机清单 + §5 范围）
2. `agate-workspace/tasks/TAG0033-codex-cmdstream-adapter/P2-design.md`（§5 五设计点定论——BDD 断言目标）
3. `agate-workspace/tasks/TAG0033-codex-cmdstream-adapter/P6-evidence/`（**整目录逐文件读**——30 个 bdd 编号日志 + 4 个 real-machine 日志 + real-machine 汇总 md）
4. `agate-workspace/tasks/TAG0033-codex-cmdstream-adapter/.state.yaml`
5. `agate-workspace/tasks/TAG0033-codex-cmdstream-adapter/gate-events.jsonl`
（角色定义文件由派发机制注入，你按其规范产出 verdict。另授 `git log` / `git diff` / `git show` 查询权——非路径注入；重跑 pytest / grep / check-*.py 允许。）

### 客观查证信息（主 Agent 已核实，2026-09-09）

- **环境**：worktree `.worktrees/agate-TAG0033`，`.state.yaml` phase=P6 / judge.enabled=true。
  HEAD = `ce62716`（P6 台账补 commit）；P6 验收 commit = `49d3353`。系统 `python3`。
- **当前测试基线**（你重跑核实）：`agate/tests/unit/` = 1390 passed / 0 failed / 2 skipped；
  `check-protocol-consistency.py --strict-errors-only`（worktree 脚本）exit 0 / 0 ERROR。
- **`P6-evidence/` 文件数**：30 个 `bdd-NN.log` + 4 个 `real-machine-*` + `real-machine-p6.md`。
- **git log 关键 commit**（`git log --oneline` 可查）：`9e91747` 立项 → `4d75958` P1 → `8fbb0d4` P2 →
  `b5f3187` P3 → `835c9b9` P4(adapter-core) → `90e00db` P4(protocol-docs) → `c00f97f` P5(首轮) →
  `52fe210` retreat P5→P4 → `9a7a1f9` DEBT0035 → `6f8422f` P4 重试#1(F1 修复) → `5f704a0` P5(重验) →
  `49d3353` P6。SELF-GATE commit（`self-gate-review:` / `self-gate-skip:` 前缀）：`835c9b9` / `90e00db` /
  `b5f3187` / `6f8422f` / `49d3353` 等。
- **P6.5 gate**（`check-gate.py P6.5`）= `check-judge-verdict.py` + `check-events.py` 双 exit 0。
  `check-judge-verdict.py` 校验：本 dispatch-context 白名单合规 + verdict frontmatter 字段完整 +
  证据交叉核对（每条结论行的 `(文件名)` 存在于 `P6-evidence/` 且 `verdict_evidence` 列表一致）。

### 产出文件字段

先 `Write` 出 `P6.5-judge-verdict.md`（frontmatter + 逐 BDD 结论 + 交叉核对节 + 总体判定），再用
`FILE=agate-workspace/tasks/TAG0033-codex-cmdstream-adapter/P6.5-judge-verdict.md python3 ~/.agate/scripts/agate-md-field-set.py --list`
查字段并逐个 `set`（`verdict_evidence` 这类列表若工具不支持可直接写 frontmatter YAML；写完
`check-frontmatter.py` 确认）。

### 返回

返回：P6.5-judge-verdict.md 路径 + `status`（passed / needs-revision）+ `criteria_passed` / 30 +
逐条是否全 PASS（否则列 FAIL BDD 编号 + 你的独立判定依据）+ 真机三态复核结论（FROZEN/NORMAL/SPIN）+
是否 `partial`。
</dispatch_guide>

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->
