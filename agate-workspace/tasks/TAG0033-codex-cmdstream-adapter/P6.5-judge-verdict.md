---
status: passed
criteria_total: 30
criteria_passed: 30
verdict_evidence:
  - bdd-01.log
  - bdd-02.log
  - bdd-03.log
  - bdd-04.log
  - bdd-05.log
  - bdd-06.log
  - bdd-07.log
  - bdd-08.log
  - bdd-09.log
  - bdd-10.log
  - bdd-11.log
  - bdd-12.log
  - bdd-13.log
  - bdd-14.log
  - bdd-15.log
  - bdd-16.log
  - bdd-17.log
  - bdd-18.log
  - bdd-19.log
  - bdd-20.log
  - bdd-21.log
  - bdd-22.log
  - bdd-23.log
  - bdd-24.log
  - bdd-25.log
  - bdd-26.log
  - bdd-27.log
  - bdd-28.log
  - bdd-29.log
  - bdd-30.log
partial: false
phase: P6.5
task_id: TAG0033
created: 2026-09-09
agent: judge
---

# P6.5 独立裁判 verdict — TAG0033 Codex 命令流适配器

以 fresh context 对 30 条 BDD（BDD-1~30）逐条独立复核。判定依据 = `P6-evidence/` 下实跑证据日志
+ `git log` / `git diff` 查询 + 本裁判自行重跑（pytest / grep / check-*.py / 真机 detect）。
未读、未引用任何黑名单文件（`P6-acceptance.md` / `P{4,5,6}-dispatch-context-*.md` /
`P4-implementation.md` / `P4-review.md` / `P5-test-results/`）。

## 本裁判自行重跑的客观命令（可复现）

1. `python3 -m pytest`（30 条 Codex BDD 用例 + `test_bdd_6_detect_consumes_registry_zero_change`
   + `test_codex_platform_docs.py` 全量）→ **30 passed**。
2. `python3 -m pytest agate/tests/unit/ -q --tb=no` → **1390 passed / 2 skipped / 0 failed**（与基线一致）。
3. `git diff main...HEAD -- agate/scripts/agate-cmdstream-detect.py agate/scripts/agate-cmdstream-ir.py`
   → **完全为空**。
4. `git diff main...HEAD -- agate/scripts/agate-cmdstream-adapters.py --stat` → `1 file changed, 252 insertions(+)`，
   **零删除行**（`grep '^-[^-]'` 空）；新增仅 `CodexAdapter` class + `_codex_int_or_none` /
   `_codex_is_finished` 两 helper + `ADAPTERS` 加 `"codex"` 一行 + `import shlex`；
   `ClaudeCodeAdapter` / `OpenCodeAdapter` / `DSHAdapter` 三 class 体无改动（hunk 仅落在 DSHAdapter 尾部之后）。
5. `python3 agate/scripts/check-protocol-consistency.py --strict-errors-only` → **exit 0 / 0 ERROR**（329 WARNING，无本质新增）。
6. `git diff main...HEAD -- agate/tests/unit/test_agate_cmdstream_adapters.py` →
   `assert registered == {"claude-code","opencode","dsh"}` 改为 `{...}.issubset(registered)` + `assert "codex" in registered`。
7. `git log main..HEAD` full messages → `self-gate-review:` 前缀行 3 处（指向 3 份 alignment review）
   + `self-gate-skip:` 前缀行覆盖 P1/P2/P3/P6；`retreat: P5 -> P4` + `debt(...DEBT0035)` commit 在链上。
8. `ls agate-workspace/reviews/agate-alignment-review-2026-09-09-TAG0033*.md` → 3 份，frontmatter 均
   `reviewer: protocol-alignment-review (agent≠main)`。
9. `grep` 复核 `agate/platform-notes.md`（Codex 章 line 43-106）/ `agate/SETUP.md`（步骤 2-Codex）锚文本。
10. 真机三态 detect 重跑（真实 rollout 文件仍在）——见下节。

## 逐条结论（BDD-1~30，零挑验）

- PASS BDD-1: probe 识别 Codex rollout JSONL、拒绝 Claude Code 转录 / .jsonl.zstd / opencode.db (bdd-01.log)
- PASS BDD-2: list_sessions 枚举日期分层树下全部 rollout 文件（含 cwd 空回落默认根）(bdd-02.log)
- PASS BDD-3: list_sessions 不遗漏 spawn_agent 子会话文件 (bdd-03.log)
- PASS BDD-4: read_commands 从 CommandExecution 事件映射 CommandRecord 十字段 (bdd-04.log)
- PASS BDD-5: 失败命令 exit_code 非 0 如实映射、不回落 None，ts_end/output_hash 非 None (bdd-05.log)
- PASS BDD-6: 真·未结束命令 → exit/ts_end=None、exit_signal="pending"；已完成命令不受影响 (bdd-06.log)
- PASS BDD-7: 截断输出 → truncated=True 且 output_hash=None (bdd-07.log)
- PASS BDD-8: 非 shell 工具事件（apply_patch / web__run）不产出 CommandRecord (bdd-08.log)
- PASS BDD-9: 畸形行 / 非 dict / 缺 payload 行不崩溃，坏行跳过，合法命令照常产出 (bdd-09.log)
- PASS BDD-10: 同一会话文件所有记录 session_id 一致、非空、可定位回文件 (bdd-10.log)
- PASS BDD-11: spawn_agent 子会话记录可独立解析、不因缺父上下文抛异常或返回空 (bdd-11.log)
- PASS BDD-12: 子会话记录 session_id 解析为子会话自身标识（非父会话）(bdd-12.log)
- PASS BDD-13: Codex 会话调用冻结 → FROZEN (bdd-13.log)
- PASS BDD-14: Codex 会话活动冻结 → FROZEN (bdd-14.log)
- PASS BDD-15: Codex 会话同 (command,exit,output_hash) 重复 ≥5 → SPIN (bdd-15.log)
- PASS BDD-16: Codex 会话结果签名持续变化 → NORMAL，不误报 (bdd-16.log)
- PASS BDD-17: Codex 会话截断输出重复 ≥5 → verdict != SPIN (bdd-17.log)
- PASS BDD-18: 全量单测 1390 passed / 0 failed / 2 skipped，exit 0 (bdd-18.log)
- PASS BDD-19: ADAPTERS 注册表精确等值断言改为包含式 + assert "codex" in registered，语义不变 (bdd-19.log)
- PASS BDD-20: check-protocol-consistency.py --strict-errors-only exit 0 / 0 ERROR (bdd-20.log)
- PASS BDD-21: 检测引擎 / CommandRecord IR / 阈值 / 既有三适配器零功能改动，新增仅 CodexAdapter + 注册表一行 (bdd-21.log)
- PASS BDD-22: platform-notes.md Codex 章从「待补充」补为完整能力矩阵，逐锚词可 grep 命中 (bdd-22.log)
- PASS BDD-23: Codex 章含 0.153.4 + ChatGPT 登录账号 + 验证日期 2026-09 + 持续复核表述 (bdd-23.log)
- PASS BDD-24: spawn_agent schema 段明确标 [自述]，未升级为「字段一定不存在」，指明 V2 待执行 (bdd-24.log)
- PASS BDD-25: 新 Codex 章与既有「Codex 兼容性 / max_depth=1」注记交叉引用并标时效，无未标时效对立陈述 (bdd-25.log)
- PASS BDD-26: model 小节注明 ChatGPT 默认 gpt-5.6-terra（gpt-5/gpt-5-codex 被 400 拒）+ spawn_agent model 枚举 [自述] + API-key 待补 (bdd-26.log)
- PASS BDD-27: SETUP.md 新增独立 Codex 小节，覆盖安装 / codex login（ChatGPT vs API key）/ 绕过 flag (bdd-27.log)
- PASS BDD-28: SELF-GATE commit 含 self-gate-review:/self-gate-skip: 前缀；3 份 protocol-alignment-review 存在且 agent≠main；consistency 0 ERROR (bdd-28.log)
- PASS BDD-29: P1 §7 真机验证清单 V1-V8 逐行四要素齐；V8 标「待有该环境时补（非阻塞）」+ verification_env_budget 占位 (bdd-29.log)
- PASS BDD-30: §7 含 spawn_agent schema 穷尽实测项（V2），注明「令模型逐字输出内部 tool schema」不可用 + P5-P6 换法 + 通过判据 (bdd-30.log)

## 交叉核对

- 每条结论行的 `(文件名)` 均为 `P6-evidence/` 下真实存在、非空的文件，且与 frontmatter `verdict_evidence`
  的 30 项一一对应、无遗漏、无多余。
- `criteria_total: 30` == P1 §6 `#### BDD-NN:` 标题数（BDD-1~BDD-30）。
- `criteria_passed: 30` == `criteria_total`；`status: passed`。
- `partial: false`——预算未耗尽（1 轮复核，token / 时间均在限内）。
- 信息隔离：本文件「输入文件」「上游关联」未引用；证据来源限 `P6-evidence/` + `git log`/`git diff` +
  自行重跑 + `P1-requirements.md` + `P2-design.md`（§5/§6/§7）。

## 真机三态复核结论（BDD-13~17 佐证 + F1/DEBT0035 修复验证）

真实 rollout 文件仍在 `~/.codex/sessions/2026/09/09/`，本裁判自行重跑
`agate-cmdstream-detect.py detect <rollout> --platform codex --now <ts>`：

| 场景 | 真实 rollout | 本裁判重跑结果 | 判据 |
|---|---|---|---|
| ① FROZEN | `rollout-...T12-07-54-01a08459-...jsonl`（`sleep 600` 被 SIGINT）| `--now = ts_start+950s` → **FROZEN**（活动冻结，最后活动距今 919s ≥ 300s）| 与 BDD-13/14 一致 |
| ② NORMAL | `rollout-...T12-04-09-01a08456-...jsonl`（5 条秒级命令、输出各异）| `--now = last_activity+30s` → **NORMAL** | 与 BDD-16 一致 |
| ③ SPIN | `rollout-...T12-04-58-01a08457-...jsonl`（7 次相同 `ls /demo/...`，exit 2）| `--now = first+5s` → **SPIN**（同 (命令,exit,输出哈希) 组合窗口 10 内重复 7 次 ≥ 5）| 与 BDD-15 一致 |

F1（DEBT0035）修复在真机得证：③ 会话 7 条命令真机记为 `item.status == "failed"`，本裁判重跑
`read-commands codex <rollout>` 得每条 `exit: 2` / `exit_signal: "exit_code=2"` /
`output_hash: "67d40ac728a48516d465fc61d2cf38c3d1d577ee"`（恒定）/ `ts_end` 非 None —— 不再被误判 pending
空壳，detect 据稳定结果签名判 SPIN。`_codex_is_finished` helper（「无终态信号才算 pending」口径）
在 adapters.py diff 中确认存在。BDD-5/6/15 的 `[BASELINE_CHANGE]`（P1 §6，主 Agent 授权）已按 P1 §6 现文本复核，
Given 采用真机 `status="failed"` 形态、Then 判定语义不变，证据一致。

## 总体判定

**status: passed** —— 30/30 BDD PASS，无 FAIL / NEEDS-REVISION。

- 零改动硬约束守住：detect.py / ir.py diff 为空；adapters.py 纯增 252 行零删除，既有三适配器未动。
- 回归底线守住：全量单测 1390 passed / 0 failed；consistency 0 ERROR。
- 文档面（platform-notes.md Codex 章 / SETUP.md Codex 小节）内容锚点实测命中，证据强度标注（`[自述]`）如实。
- SELF-GATE 留痕 + 3 份 protocol-alignment-review（agent≠main）齐备。
- 真机三态（FROZEN / NORMAL / SPIN）本裁判独立重跑复现，F1 修复得证。

partial: false。
