---
phase: P6.5
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0037
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）
> 本次是 P6.5 独立 judge 复核首次派发（fresh context，信息隔离）。

### 目标

对 P1 的**全部** 52 条 BDD 逐条独立重验（零挑验，含所有条目），只凭标准与证据，产出 `P6.5-judge-verdict.md`：frontmatter 含 `status`（passed/rejected/needs-revision）、`criteria_total`（须等于 P1 `#### BDD-NN:` 标题数）、`criteria_passed`、`verdict_evidence`（证据清单，每条结论引用须为其子集且逐条被引用）、`partial`；正文逐 BDD 一行结论，格式 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: 描述 (证据路径)`，路径相对任务目录（如 `P6-evidence/g1/bdd-01-pytest.log`），指向真实存在且非空的文件。

### 约束

1. 判案标准 = P1 每条 BDD 的 Given/When/Then 全部 Then 子项（含 P1 中的 `[BASELINE_CHANGE]` 注记，它们是本任务已授权的范围口径，与 BDD 正文一并适用）+ P2 验收相关节；判案依据只能是 `P6-evidence/` 下证据文件内容、`gate-events.jsonl`、`.state.yaml` 与 git log 可查证的记录。证据不足以判定 → 判 `FAIL` 或 `NEEDS-REVISION`，不猜、不放过；不接受"看起来没问题"。
2. 每条 BDD 的**全部 Then 子项**都要在证据里找到对应事实（例如参数化 BDD 的每个子场景、BDD-20 的 ①②③ 与清理清单、BDD-23 的数值判据 `S ≤ 1.25×B`、BDD-37 的命中数与白名单）；只看到"pytest 通过"而 BDD 要求命令面/真实环境实测者，须核对对应实测证据。
3. 你有 git log 查询权（例如核对提交存在性、涉及文件、作者）；可对证据文件做只读读取与 grep。
4. **数据安全（用户强制）**：只读复核；除输出文件 `P6.5-judge-verdict.md` 外不得新增/修改/删除任何仓库文件；禁止 `rm -rf`/清空重建；不 git add/commit/push/tag；不触碰真实 ~/.agate、开发 checkout、GitHub（不删除、不推送任何 ref）；如需临时文件仅用 scratchpad 新建带序号目录。
5. 预算：复核轮次 ≤2、token ≤300k（`.state.yaml` judge.judge_token_budget）、时间 30 分钟；预算将尽时按已验条目落盘并置 `status: needs-revision` + `partial: true`，说明未验条目，不得静默放行。
6. 返回：`File: <路径>` + `Status: <passed|rejected|needs-revision>`（两行）。

### 上游关联

- 任务目录 `agate-workspace/tasks/TAG0037-install-package-model/` 下的 `P1-requirements.md`、`P2-design.md`（仅验收相关节：§3、§11、§14）、`P6-evidence/`、`.state.yaml`、`gate-events.jsonl`

### 输入文件

- {AGATE_WORKSPACE}/tasks/TAG0037-install-package-model/P1-requirements.md
- {AGATE_WORKSPACE}/tasks/TAG0037-install-package-model/P2-design.md
- {AGATE_WORKSPACE}/tasks/TAG0037-install-package-model/P6-evidence/
- {AGATE_WORKSPACE}/tasks/TAG0037-install-package-model/.state.yaml
- {AGATE_WORKSPACE}/tasks/TAG0037-install-package-model/gate-events.jsonl
- 产出：{AGATE_WORKSPACE}/tasks/TAG0037-install-package-model/P6.5-judge-verdict.md
</dispatch_guide>

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->
