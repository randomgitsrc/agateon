---
phase: P6.5
task_id: TAG0032
parent: P1-requirements.md
trace_id: TAG0032-P65-20260907
agent: judge
type: judge-verdict
status: passed
criteria_total: 14
criteria_passed: 14
verdict_evidence: ["bdd-1-install-symlink.log", "bdd-2-migration-hint.log", "bdd-3-plain-dir.log", "bdd-4-root-scripts.log", "bdd-4-doc-semantics.log", "bdd-5-install-sh-versions.log", "bdd-5-curl-bash-transcript.txt", "bdd-6-meta-resolve.log", "bdd-7-rootproto-resolve.log", "bdd-8-hook-gate-path.log", "bdd-9-full-suite.log", "bdd-10-update-aligned.log", "bdd-11-lifecycle-section.log", "bdd-12-doc-converged.log", "bdd-12-consistency.log", "bdd-13-e2e-lifecycle.log", "bdd-14-no-pollution.log"]
partial: false
created: 2026-09-07
---

# P6.5-judge-verdict — TAG0032 版本管理生命周期可用性批（fresh-context 独立裁判）

[PROD_NOT_TOUCHED]

## 判案方法

- 逐条重验 P1-requirements.md §3 全部 14 条 BDD（零挑验，含 P6 已判 PASS 项），只凭 P1 §3 Given/When/Then 原文 +
  `P6-evidence/` 证据文件内容 + git log。
- 每条证据文件为该 BDD 的 `pytest -v` 实跑输出（测试名 + `PASSED` + `EXIT_CODE 0` 尾行）；断言覆盖度对照
  证据日志各自具名的已提交测试源（P4 实现 commit `1d9a322`，author `Test <test@test.local>`）交叉核对——
  测试断言逐条命中对应 BDD 的判据。
- git log 交叉：P4 `1d9a322` / P5 `2f50f4c` / P6 `af5bfe6` 三 commit 均存在、author 一致（`Test <test@test.local>`）；
  `gate-events.jsonl` 事件账本 state_transition 链推进至 P6，无 `budget_exhausted` 事件。
- 预算：复核 1 轮 / token 与时间均未耗尽 → 无降级，`partial: false`。

## 逐条结论（14 条，零挑验）

- PASS BDD-1: legacy 软链布局 install fail-closed 拒绝（exit≠0），软链目标内不出现新建 repo/ 与 vX.Y.Z/——test_tag0032_bdd_1 断言 returncode!=0 + not (legacy_target/repo).exists() + not _dir_has_version_subdir，EXIT_CODE 0 (bdd-1-install-symlink.log)
- PASS BDD-2: 拒绝 stderr 含三段可 grep 命令片段迁移指引（mv ~/.agate +.bak/.old、mkdir -p ~/.agate、agate-install.py latest|vX.Y.Z 或 install.sh --versions），三者缺一即 FAIL——test_tag0032_bdd_2 三断言全过，EXIT_CODE 0 (bdd-2-migration-hint.log)
- PASS BDD-3: 普通（非软链）目录布局 install 不被新拒绝逻辑误伤，exit 0 且 ~/.agate/v0.48.0/ 建立成功——test_tag0032_bdd_3 断言 returncode==0 + version_dir.is_dir()，EXIT_CODE 0 (bdd-3-plain-dir.log)
- PASS BDD-4: install 后根 ~/.agate/scripts/agate-install.py 存在且 --help exit 0（判据1）；tests/ 含「重跑 latest 后根 scripts/ 副本随 current 刷新」用例（判据2，SENTINEL 断言）；该用例维护语义与 UPGRADING 生命周期节一致（判据3：副本 + 重跑 agate-install 刷新 + repo/ 被删影响）——test_tag0032_bdd_4 + bdd_4b + bdd_4_root_scripts_copy_semantics_documented 全过，EXIT_CODE 0 (bdd-4-root-scripts.log, bdd-4-doc-semantics.log)
- PASS BDD-5: 新机器按 install.sh --versions 官方路径从零进版本布局（repo/ + vX.Y.Z/ + current/latest 指针 + 根 scripts/），且不污染源仓库树——test_tag0032_bdd_5 用例过 + 隔离 HOME 真实 `cat install.sh | bash -s -- --versions` transcript EXIT_CODE 0，~/.agate 全布局建立，指针 current→latest→v0.50.0，根 scripts/agate-install.py --help EXIT 0，worktree 内无 repo/ 或 vX.Y.Z/ (bdd-5-install-sh-versions.log, bdd-5-curl-bash-transcript.txt)
- PASS BDD-6: 元仓库形态版本目录 resolve 返回协议子目录 vdir/agate，且 AGATE_VERSION 仍为 vX.Y.Z（版本号不回归 I-1）——test_tag0032_bdd_6 断言 AGATE_ROOT={vdir}/agate + AGATE_VERSION=v0.50.0，EXIT_CODE 0 (bdd-6-meta-resolve.log)
- PASS BDD-7: 「根即协议」部署方解析语义不变（探测序 vdir/scripts 先命中 → 返回 vdir，不进 vdir/agate 分支），AGATE_VERSION 仍为 vX.Y.Z（纯增量红线）——test_tag0032_bdd_7 断言 _protocol_root(vdir)==vdir + AGATE_ROOT={vdir}（非 /agate）+ AGATE_VERSION=v0.50.0，EXIT_CODE 0 (bdd-7-rootproto-resolve.log)
- PASS BDD-8: 元仓库形态钉版后 resolve-entry.py pre-commit 解析出的 gate 路径 <root>/scripts/pre-commit-gate.py（root=vdir/agate）存在并被 exec，不因 gate 缺失 exit 1——test_tag0032_bdd_8 断言 returncode==0 + stub marker GATE-META-050 在输出，EXIT_CODE 0 (bdd-8-hook-gate-path.log)
- PASS BDD-9: 两形态并存下 unit/regression/integration 全量 pytest -n auto 全绿（含新增元仓库形态与根即协议形态用例，DEBT0016 解析旁路回归锁）——全量汇总 1484 passed, 2 skipped，EXIT_CODE 0（2 skip 与 I-6 GitHub clone 增强降级一致，无 FAIL） (bdd-9-full-suite.log)
- PASS BDD-10: UPGRADING「版本管理生命周期」节内 legacy 布局更新明确为 git pull（含是否重跑 install-hook.py 判定口径），版本布局明确为 agate-install latest 并声明幂等——test_tag0032_bdd_10 断言 sec 含 git pull + install-hook.py + agate-install latest + 幂等，EXIT_CODE 0 (bdd-10-update-aligned.log)
- PASS BDD-11: 生命周期节四动作（安装/迁移/更新/回退）均有命令或步骤 + 写明 hook 重装时机（薄壳固定/resolve-entry 机制下通常无需随版本重装）+ 含根 ~/.agate/scripts/ 副本维护语义条目，缺一即 FAIL——test_tag0032_bdd_11 逐项断言全过，EXIT_CODE 0 (bdd-11-lifecycle-section.log)
- PASS BDD-12: §4 扫描 4 的 4 条矛盾表述逐条命中「已收敛」或「历史叙事保留+指针」（v0.50.0 表格根 scripts/ 行 + 升级行加生命周期节指针；v0.60-0.62 vs v0.66-0.68 hook 口径由生命周期节统一且历史节不逐条改写；README×2 补 install.sh --versions + SETUP 加指针），无第三态；附加回归 check-protocol-consistency.py --strict-errors-only EXIT 0 / 0 ERROR——test_tag0032_bdd_12 四参数用例全过 + consistency 日志「无 ERROR」EXIT_CODE 0 (bdd-12-doc-converged.log, bdd-12-consistency.log)
- PASS BDD-13: 隔离 HOME 全链路逐步 exit 符合期望（进入版本布局 exit0 + repo//指针；装版本 exit0 + 版本目录唯一 + 根 scripts/；钉 .agate-version exit0；resolve-entry pre-commit exit0 + stub gate exec；再次 agate-install latest exit0 且版本目录数量不变），本地元仓库形态 fixture（CI 无网 fallback I-6）——test_tag0032_bdd_13 五步断言全过，EXIT_CODE 0 (bdd-13-e2e-lifecycle.log)
- PASS BDD-14: 端到端全链路执行完毕后 worktree 源仓库树无新增 repo/ 主克隆、无 vX.Y.Z/ worktree、git status --porcelain 无非预期未跟踪条目——test_tag0032_bdd_14 断言 porcelain 无 /repo/ 或 /vX.Y.Z/ offender + repo_root/agate 内无 repo/ 与 vX.Y.Z/，EXIT_CODE 0（隔离 HOME transcript 亦佐证「无」） (bdd-14-no-pollution.log)

## 裁定

14/14 BDD 独立重验均 PASS，证据文件均真实存在、非空、md5 互异，git log 三 commit 存在且 author 一致，
事件账本无预算耗尽降级。→ **status: passed**（映射 approved，P6→P7 放行）。
