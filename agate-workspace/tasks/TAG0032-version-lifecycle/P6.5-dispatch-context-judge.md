---
phase: P6.5
generated_by: 主 Agent
task_id: TAG0032
role: judge
---

<dispatch_guide>
> ⚠️ 派发指引是强制指令。执行优先级：派发指引 > 客观查证信息 > 阶段卡片。

### 目标

P6.5 验收独立裁判：以 **fresh context** 逐条重验 P1-requirements.md 的**全部 14 条 BDD**（零挑验，含 P6 已判 PASS 项），只凭 P1 BDD 标准 + `P6-evidence/` 证据文件 + git log 判案。产出 `P6.5-judge-verdict.md`。

### 认知约束（四条强制）

1. **逐条重验所有 14 条 BDD**：P1 有 14 条 `#### BDD-NN:` → verdict 必须给出 14 条独立结论。挑验即违规（`check-judge-verdict.py` 机械拦截）。
2. **只信证据与 git log，不信叙述**：每条结论的依据只能是 `P6-evidence/` 下证据文件的内容 + git log 可查证记录。**不读、不引用** verifier 的验收自述文件（不在你的输入白名单里，也不要去找）。
3. **每条结论引用证据路径**：格式 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: {描述} ({证据路径})`；引用须在 `verdict_evidence` 清单内且指向 `P6-evidence/` 下真实存在、非空的文件。
4. **禁"看起来没问题"式结论**：证据不足以判定 → 判 `FAIL` 或 `NEEDS-REVISION`，不猜、不放过。

### 证据文件清单（P6-evidence/ 目录，你自己读）

- `bdd-1-install-symlink.log` … `bdd-14-no-pollution.log`（各 BDD 的 pytest -v 实跑输出 + EXIT_CODE 尾行）
- `bdd-4-doc-semantics.log` / `bdd-4-root-scripts.log`（BDD-4 两份）
- `bdd-5-install-sh-versions.log` / `bdd-5-curl-bash-transcript.txt`（BDD-5 两份——后者是隔离 HOME 真实 `cat install.sh | bash -s -- --versions` transcript）
- `bdd-12-doc-converged.log` / `bdd-12-consistency.log`（BDD-12 两份）
- `bdd-9-full-suite.log`（全量 pytest 汇总）

逐条判：证据文件里的 pytest 结果（`N passed` / `EXIT_CODE 0`）+ 断言内容是否覆盖该 BDD 的 Given/When/Then 判据（对照 P1 §3 原文，尤其 BDD-2 三段命令片段、BDD-4 三判据、BDD-6/7 的 `AGATE_VERSION` 不回归、BDD-12 四条 checklist、BDD-13 逐步 exit、BDD-14 无污染）。证据不足以覆盖某条判据 → 该 BDD 判 FAIL/NEEDS-REVISION。

### git log 交叉核对（另授查询权）

可 `git log` / `git show` 查证：P4 实现 commit `1d9a322`、P5 `2f50f4c`、P6 `af5bfe6` 存在且 author 一致；`gate-events.jsonl` 事件账本可交叉核对执行留痕。

### 三档预算（诚实降级）

- 复核轮次 ≤2；token 默认 100k；时间 30min。任一耗尽 → 立即停止，按已验条目落盘 verdict，**必须 `status: needs-revision` + `partial: true`**，结论区说明哪些条目未验完。**不得** `status: passed` 静默放行。

### 输出

产出 `{AGATE_WORKSPACE}/tasks/TAG0032-version-lifecycle/P6.5-judge-verdict.md`：
- Header：`status`（passed / rejected / needs-revision）+ `criteria_total: 14`（必须 == P1 `#### BDD-NN:` 标题数）+ `criteria_passed: N`（status=passed 时须 == 14）+ `verdict_evidence: [JSON 列表]`（每条结论引用 ⊆ 此清单且逐条被引用）+ `partial: false`
- 正文：14 条结论行 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: 描述 (证据路径)`

### 上游关联

任务 TAG0032：版本管理生命周期可用性批（P1 §3 定义 14 条 BDD——断点一入口断链 BDD-1~5、断点二元仓库 gap BDD-6~9、断点三 update 入口 BDD-10~12、端到端 BDD-13~14）。`.state.yaml` `judge.enabled: true`，`risk_level: high`。

### 输入文件（白名单——只读这些）

- {AGATE_WORKSPACE}/tasks/TAG0032-version-lifecycle/P1-requirements.md（14 条 BDD 标准，逐条判案唯一依据——读 §3 全文）
- {AGATE_WORKSPACE}/tasks/TAG0032-version-lifecycle/P2-design.md（仅 §10「实现完成标志」+ §4「其他覆盖点」验收相关节——BDD 判据交叉参考，不读 §1 影响面/§7 files_to_read）
- {AGATE_WORKSPACE}/tasks/TAG0032-version-lifecycle/P6-evidence/（证据文件目录——逐个读）
- {AGATE_WORKSPACE}/tasks/TAG0032-version-lifecycle/.state.yaml（任务状态）
- {AGATE_WORKSPACE}/tasks/TAG0032-version-lifecycle/gate-events.jsonl（事件账本）
- git log / git show 查询权（worktree `/home/kity/oclab/agateon/.worktrees/agate-TAG0032`）

路径说明：{AGATE_WORKSPACE} = `/home/kity/oclab/agateon/.worktrees/agate-TAG0032/agate-workspace`。角色定义已在派发 prompt 指明，无需再从本节读取。

### 客观查证信息

- 解释器 `/usr/bin/python3`；只读判案，不改任何文件、不碰 git 历史
- bash 外层 `timeout` 30-90s；单步串行；状态标记 `[PROD_NOT_TOUCHED]`
- 产出路径硬约束：写 `{AGATE_WORKSPACE}/tasks/TAG0032-version-lifecycle/P6.5-judge-verdict.md`
- frontmatter 用 `agate-md-field-set.py` 能填的填；不支持的用 Write 直接写。Header 成品值：`phase: P6.5` / `task_id: TAG0032` / `parent: P1-requirements.md` / `trace_id: TAG0032-P65-20260907` / `agent: judge` / `type: judge-verdict`

### 返回

`File: <P6.5-judge-verdict.md 路径>` + `Status: <passed|rejected|needs-revision>` + 一句话摘要（≤30 字：criteria_passed/14）。不返回文件全文。
</dispatch_guide>
