---
phase: P6.5
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0036
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）
> 本次是 P6.5 首次派发（非重试）。你是独立 judge，fresh context，不预设验收结论。**信息隔离强制要求**：本 dispatch-context 只引用白名单路径（`P1-requirements.md` / `P2-design.md` 验收相关节 / `P6-evidence/` 目录 / `.state.yaml` / `gate-events.jsonl`），另授 git log 查询权。

### 目标

独立、从零开始逐条重验 `P1-requirements.md` 的**全部 71 条 BDD**（含所有可能已被声明通过的项，零挑验），只信 `P6-evidence/` 目录下的真实证据文件与 git log 可查证的执行留痕，产出 `P6.5-judge-verdict.md`。

### 约束

1. **逐条重验所有 71 条 BDD**：对每条 BDD，读 `P6-evidence/` 下对应的证据文件，核实证据内容是否**真实支撑**该条 BDD 的 Given/When/Then（命令原文、实际输出、期望值比对），不能假设"文件存在即通过"。BDD 数 = `P1-requirements.md` 中 `#### BDD-NN:` 标题数；`criteria_total` 必须与之相等。
2. **认知约束**：只信证据与 git log；禁止"看起来没问题"式结论；证据不足以判定 → 判 FAIL 或 NEEDS-REVISION，不猜不放过。每条结论必须引用 `P6-evidence/` 下**真实存在且非空**的证据文件（引用路径写成 `P6-evidence/<文件名>` 的同一形式，且全部列入 `verdict_evidence`，每个列入的文件须被至少一条结论引用）。
3. **重点独立审视（不预设答案）**：
   - 「每条证据是否真的对应该 BDD 的 Then，而不是泛泛的 pytest 全绿」——尤其行为类 BDD 是否有独立于测试代码的直接实证（如哨兵未执行需有**对照组**）；
   - 全量套件类证据（尾行 `EXIT_CODE`）、零内核 diff 类证据（命令原文 + 空 diff + `EXIT_CODE: 0`）是否自洽，命令是否逐字取自 P2-design.md 的 gate_commands 声明；
   - BDD-56（真实任务目录跑 `--observe`）的输出是否满足 BDD-43 的 7 列结构、行数是否 = 该任务批数、运行前后快照是否一致；
   - ⑤ 组只验「成文到位」——若发现证据在断言"机制已生效"，应判 NEEDS-REVISION；
   - 用 `git log` 交叉核实：P4 实现 commit、P5、P6 commit 确实存在且顺序合理；`gate-events.jsonl` 与 `.state.yaml` 的 `p5_pass_commit` 是否与 git 历史吻合。
4. **三档预算**：轮次 ≤2 / token：本任务因 BDD 数 71，`.state.yaml` 的 `judge.judge_token_budget` 已覆盖为 400000 / 时间 30 分钟。**任一预算耗尽 → 立即停止，按已验条目落盘 verdict，必须 `status: needs-revision` + `partial: true`**，并在结论区说明哪些条目未验完；禁止以 `status: passed` 静默放行。请先规划：先通读 `P1-requirements.md`（BDD 标准），再按证据文件分组核验，**尽早分阶段落盘**（可先写出已验条目再补全），避免中断丢失。
5. **verdict 产出格式**（`P6.5-judge-verdict.md`）：Header 用 `agate-md-field-set.py` 或等价方式写 `status`（passed / rejected / needs-revision）、`criteria_total`（=71）、`criteria_passed`、`verdict_evidence`（JSON 列表，格式 `["P6-evidence/xxx.log", ...]`）、`partial`（false，超限时 true）；正文每条 BDD 一行，行首格式逐字为 `- PASS BDD-NN: {描述} ({证据路径})` / `- FAIL BDD-NN: ...` / `- NEEDS-REVISION BDD-NN: ...`，零挑验、71 行。总结行不得以 `- PASS`/`- FAIL` 开头。`status: passed` 仅当 71 条全为 PASS。
6. **不得读取本 dispatch-context 所列白名单之外的任何任务产出文件**（任务目录下其他文件均不在你的判案依据内；`P1-requirements.md` 与 `P2-design.md` 也仅作标准/验收相关声明使用）。你也不得修改任何文件（除写 `P6.5-judge-verdict.md`），不 git add/commit；只读 git（`git log` / `git show --stat` / `git diff --stat` 等）。
7. 只报告 `File: <路径>` + `Status: <passed|rejected|needs-revision>`，不声称"任务完成"。

### 上游关联

- `P1-requirements.md`：71 条 BDD 原文（`#### BDD-NN:` 编号 + Given/When/Then + 口径 A-H），本次复核的验收基线
- `P2-design.md`：仅读「gate_commands」（§7）与「实现完成的标志」（§12）等验收相关节，用于核对证据中命令是否与声明一致；不需要通读候选方案对比等设计过程
- `.state.yaml`：任务状态字段（`p5_pass_commit`、`judge` 等）
- `gate-events.jsonl`：append-only 事件账本，可交叉核实执行留痕

### 输入文件

- {AGATE_WORKSPACE}/tasks/TAG0036-mvwu-pilot/P1-requirements.md
- {AGATE_WORKSPACE}/tasks/TAG0036-mvwu-pilot/P2-design.md
- {AGATE_WORKSPACE}/tasks/TAG0036-mvwu-pilot/P6-evidence/（目录下全部 31 个证据文件）
- {AGATE_WORKSPACE}/tasks/TAG0036-mvwu-pilot/.state.yaml
- {AGATE_WORKSPACE}/tasks/TAG0036-mvwu-pilot/gate-events.jsonl
- 产出路径：{AGATE_WORKSPACE}/tasks/TAG0036-mvwu-pilot/P6.5-judge-verdict.md
</dispatch_guide>

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->

<objective_info>
- 仓库：worktree `/home/kity/oclab/agateon/.worktrees/agate-TAG0036`，分支 `feat/TAG0036-mvwu-pilot`；基线 `main` = `efb113b`。
- git 历史（可用 `git log --oneline` 交叉核实）：各阶段（P0 至 P6，逐阶段）以 `wf(TAG0036-P{N}):` 前缀提交。
</objective_info>
