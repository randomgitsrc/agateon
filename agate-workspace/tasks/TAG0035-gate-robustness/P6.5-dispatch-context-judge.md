---
phase: P6
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0035
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）
> 本次是 P6.5 首次派发（非重试）。你是独立 judge，fresh context，不预设验收结论。**信息隔离强制要求**：本 dispatch-context 只允许引用白名单路径（`P1-requirements.md` / `P2-design.md` 验收相关节 / `P6-evidence/` 目录 / `.state.yaml` / `gate-events.jsonl` / 角色定义文件路径），已排除任何 verifier/implementer 的自述文件与派发上下文。你不应尝试读取本 dispatch-context 未列出的其他任务产出文件。

### 目标

独立、从零开始逐条重验 P1-requirements.md 的全部 14 条 BDD（含所有已声明 PASS 的项，零挑验），只信 `P6-evidence/` 目录下的真实证据文件与 git log 可查证的执行留痕，产出 `P6.5-judge-verdict.md`。

### 约束

1. **逐条重验所有 14 条 BDD**：对每条 BDD，读 `P6-evidence/` 目录下对应的证据文件（4 个批次日志 + 1 个手工 CLI 日志），核实证据内容是否真实支撑该条 BDD 的 Given/When/Then 判定，不能假设"文件存在即通过"。
2. **认知约束**：只信证据与 git log。禁止"看起来没问题"式结论；每条结论必须引用 `P6-evidence/` 下真实存在且非空的证据文件。git log 可查证执行留痕（如 `git log --oneline` 确认对应 commit 确实落地了代码改动）。
3. **三档预算**：轮次 ≤2 / token 100k / 时间 30min。任一预算耗尽 → 立即停止，按已验条目落盘 verdict，必须 `status: needs-revision` + `partial: true`，禁止 `status: passed` 静默放行。
4. **verdict 产出格式**：Header 含 `status`（passed/rejected/needs-revision）+ `criteria_total`（=14）+ `criteria_passed` + `verdict_evidence`（JSON 列表，引用你核实过的证据文件路径）+（可选）`partial`；正文每条 BDD 一行 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: 描述 (证据路径)`。
5. **不得读取本 dispatch-context 之外的任何任务产出文件**——你的判断依据仅限白名单路径 + git log 查询。

### 上游关联

- P1-requirements.md：14 条 BDD 原文（`#### BDD-NN:` 编号 + Given/When/Then + 验证命令），本次复核的验收基线
- P2-design.md：仅需读取「gate_commands」「files_to_read」等验收相关声明节，理解每条 BDD 对应的技术方案是否与证据吻合，不需要通读候选方案对比等设计过程细节
- `.state.yaml`：任务当前状态字段（`p5_pass_commit` 等）
- `gate-events.jsonl`：append-only 事件账本，可用于交叉核实执行留痕

### 输入文件

- {AGATE_WORKSPACE}/tasks/TAG0035-gate-robustness/P1-requirements.md
- {AGATE_WORKSPACE}/tasks/TAG0035-gate-robustness/P2-design.md
- {AGATE_WORKSPACE}/tasks/TAG0035-gate-robustness/P6-evidence/（目录下全部 5 个证据文件）
- {AGATE_WORKSPACE}/tasks/TAG0035-gate-robustness/.state.yaml
- {AGATE_WORKSPACE}/tasks/TAG0035-gate-robustness/gate-events.jsonl
</dispatch_guide>

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->

<objective_info>
- 环境：worktree 分支 feat/TAG0035-gate-robustness；python3 3.12.3 / pytest 9.0.3
- git log 可用于交叉核实：`git log --oneline` 可查到本任务的 5 个 wf(TAG0035-P4)/wf(TAG0035-P5)/wf(TAG0035-P6) commit 序列
</objective_info>
